# About

**This site has moved to <https://sandeepbazar.github.io/>.**

The personal site is now the GitHub user site, served from the `sandeepbazar.github.io`
repository. Edit it there, not here.

What is left in this repository:

- `index.html`, a redirect to the root domain, so older links still resolve.
- `assets/`, the two images only.

The resume PDF was removed. It carried a phone number, an email address and a full postal
location in its header, and it was publicly downloadable. Note that removing the file stops
further downloads but does not retract copies already crawled, and the blob remains reachable
in this repository's git history.

Nothing else here is maintained.
